import 'vue-image-zoomer'; // Import the library directly to register the directive globally

export default defineNuxtPlugin((nuxtApp) => {
  // The v-image-zoom directive should now be available
});